{
  "version": 1,
  "branch": "@cli",
  "jobs": [
    {
      "id": "wake-test",
      "enabled": false,
      "schedule": {
        "type": "interval",
        "interval_minutes": 5
      },
      "wake": {
        "fresh": true
      },
      "prompt": "AUTOMATED DAEMON TEST. Do ONLY this: run drone @ai_mail email @devpulse \"DAEMON TEST \u2014 cli\" \"Woke via @daemon systemd timer. No memory touched.\" then STOP. Do NOT run startup, do NOT update memory, do NOT do anything else."
    }
  ]
}
